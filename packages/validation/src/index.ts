/**
 * @palantir/validation
 *
 * Zod-Schemas, die Backend (Request-Validierung) und Frontend (Formular-/
 * Typprüfung) gemeinsam nutzen – Pflichtenheft §3 und §4.
 *
 * Dieses Package ist im Grundgerüst bewusst leer. Schemas werden zusammen mit
 * den zugehörigen Typen aus `@palantir/contracts` über eigene, kleine PRs
 * ergänzt (CLAUDE.md §3 und §6).
 */

export {};
