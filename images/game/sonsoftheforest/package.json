{
  "name": "@palantir/sonsoftheforest-image",
  "version": "0.0.0",
  "private": true,
  "description": "Sons-of-the-Forest-Spiel-Image. Diese package.json existiert nur, damit `pnpm test` im Repo-Wurzelverzeichnis die Tests der Shell-Skripte mitlaufen laesst; ins Image kopiert wird sie nicht (siehe .dockerignore).",
  "scripts": {
    "lint": "eslint .",
    "test": "node --test"
  }
}
