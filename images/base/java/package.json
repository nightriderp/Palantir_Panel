{
  "name": "@palantir/base-java-image",
  "version": "0.0.0",
  "private": true,
  "description": "Basis-Image für Java-Spiele. Diese package.json existiert nur, damit `pnpm test` im Repo-Wurzelverzeichnis die Tests der Shell-Bibliothek mitlaufen lässt; ins Image kopiert wird sie nicht (siehe .dockerignore).",
  "scripts": {
    "lint": "eslint .",
    "test": "node --test"
  }
}
