import { SkinsView } from '@/components/skins/SkinsView';

export const metadata = {
  title: 'Skins · Palantir',
};

/** Skins (Arbeitspaket F9, Lastenheft §7) – Phase-1-Platzhalter. */
export default function SkinsPage() {
  return <SkinsView />;
}
