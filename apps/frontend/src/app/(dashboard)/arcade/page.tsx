import { ArcadeView } from '@/components/arcade/ArcadeView';

export const metadata = {
  title: 'Arcade · Palantir',
};

/** Arcade – Minispiele mit Bestenliste (Arbeitspaket F8, Lastenheft §3.9). */
export default function ArcadePage() {
  return <ArcadeView />;
}
